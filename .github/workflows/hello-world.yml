name: Hello World Workflow
on: 
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  hello-world-job:
    runs-on: ubuntu-latest

    steps:
    - name: test
      run: exit 1
   # - name: Print Hello World
    #  run: echo "Hello World"
